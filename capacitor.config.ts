import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.entropylab.Fallenleaves',
  appName: 'Fallen leaves',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
